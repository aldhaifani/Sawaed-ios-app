import SwiftUI

struct DashboardView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Dashboard")
            Text("Learning progress")
            Text("Suggested opportunities")
        }
        .padding()
    }
}
